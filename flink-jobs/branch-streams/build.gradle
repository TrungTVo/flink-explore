dependencies {
    flinkShadowJar project(":common")

    // Example: add libraries needed only by this job
}

shadowJar {
    archiveBaseName.set("branch-streams")
    // Ensure manifest has a Main-Class so you can run it as an application artifact if needed
    // manifest {
    //     attributes "Main-Class": "org.quickstart.WordCount"
    // }
}

application {
    mainClass = 'org.quickstart.BranchStreams'
    applicationDefaultJvmArgs = ["-Dlog4j.configurationFile=log4j2.properties"]
}
